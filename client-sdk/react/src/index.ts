/**
 * @jonpepler/kerbcam-react
 *
 * React components and hooks for kerbcam camera feeds. This package
 * is a peer to @jonpepler/kerbcam (the core SDK) and requires React 18+
 * and styled-components 6+ as peer dependencies.
 *
 * Components are extracted from gonogo's camera feed UI; this module
 * is the shared layer both gonogo and the sidecar embedded page consume.
 */

// Components and hooks are added here as part of the sidecar page revamp.
// This placeholder satisfies the package shape so dependents can reference
// the package before the first component lands.
export {};
