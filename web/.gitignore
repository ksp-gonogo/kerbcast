# Ignore all build output in dist/ except the pre-built self-contained page
# that the sidecar binary embeds via include_str!. The built file is committed
# so cargo builds need no Node toolchain.
dist/*
!dist/index.html

node_modules/
